/// Barrel: Create Post now lives in the `features/create_post` module
/// (domain/data/services/presentation split, Riverpod state management).
/// Kept here so existing imports/navigation (e.g. `HomePage.dart`) don't
/// need to change.
library;

export 'package:halo/features/create_post/presentation/add_post_page.dart';
